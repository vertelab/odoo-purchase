<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <record id="view_purchase_order_form_view" model="ir.ui.view">
            <field name="name">purchase.order.form.view</field>
            <field name="model">purchase.order</field>
            <field name="inherit_id" ref="purchase.purchase_order_form"/>
            <field name="arch" type="xml">
                 <button name="button_draft" position="attributes">
                    <attribute name="confirm">Are you sure to set this to quotation?</attribute>
                </button>
                <button name="button_cancel" position="attributes">
                    <attribute name="confirm">Are you sure to cancel this order?</attribute>
                </button>
            </field>
        </record>
    </data>
</odoo>
